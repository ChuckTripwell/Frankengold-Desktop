# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: frankengold-desktop
description: WIP
image-version: latest

base-image: quay.io/fedora/fedora-kinoite

modules:
  - type: containerfile
    source: Containerfile
